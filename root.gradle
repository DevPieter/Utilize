plugins {
    id 'fabric-loom' version '1.10-SNAPSHOT' apply false
    id 'com.replaymod.preprocess' version '7c980d91ab'
}

preprocess {
    def mc12102 = createNode('1.21.2', 12102, "yarn")
    def mc12006 = createNode('1.20.6', 12006, "yarn")

    mc12102.link(mc12006, null)
}